local opts = {
    settings = {
        yaml = {
            shemas = {
                ["$XDG_CONFIG_HOME/nvim/lua/user/plugins/lsp/schemas/ansible.json"] = "/*.yml"
            }
        }
    }
}

return opts

